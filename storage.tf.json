{
  "resource": {
    "oci_objectstorage_bucket": {
      "state": {
        "compartment_id": "${oci_identity_compartment.platform.id}",
        "name": "${local.project}-bucket-state",
        "namespace": "${data.oci_objectstorage_namespace.state.namespace}"
      }
    }
  }
}
